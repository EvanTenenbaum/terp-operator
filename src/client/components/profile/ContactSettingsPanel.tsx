import type { ContactProfileData } from './types';

interface Props { data: ContactProfileData; }

// Phase 8 stub — full implementation coming in next phase
export function ContactSettingsPanel({ data: _data }: Props) {
  return (
    <div className="p-4 text-sm text-zinc-400">Settings panel — coming in Phase 8.</div>
  );
}
