interface Props { contactId: string; }

// Phase 8 stub — full implementation coming in next phase
export function ContactAppointmentsPanel({ contactId: _contactId }: Props) {
  return (
    <div className="p-4 text-sm text-zinc-400">Appointments panel — coming in Phase 8.</div>
  );
}
