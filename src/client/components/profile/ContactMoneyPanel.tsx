import type { ContactProfileData } from './types';

interface Props { data: ContactProfileData; }

// Phase 8 stub — full implementation coming in next phase
export function ContactMoneyPanel({ data: _data }: Props) {
  return (
    <div className="p-4 text-sm text-zinc-400">Money panel — coming in Phase 8.</div>
  );
}
